spring:
  cloud:
    config:
      uri: http://127.0.0.1:5500
    discovery:
      client:
        serviceUrl:
          defaultZone: http://localhost:8761/eureka/
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true